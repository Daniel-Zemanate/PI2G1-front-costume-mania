/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'http2.mlstatic.com', 'costumemania.s3.amazonaws.com'],
  },
  reactStrictMode: true,
}

module.exports = nextConfig
